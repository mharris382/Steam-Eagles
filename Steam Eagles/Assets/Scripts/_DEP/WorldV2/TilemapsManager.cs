using System;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.Tilemaps;
#if UNITY_EDITOR
using UnityEditor;
#endif

namespace WorldV2
{

    /// <summary>
    /// If we want to make it so that the tilemap the player builds on is selected contextually based on their location, we need to establish the mechanisms for doing so. 
    /// </summary>
    public class TilemapBuildingArea : MonoBehaviour
    {
        
    }
    
}